<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.awesome.programmiernacht" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".selectNumberOfGroups"
            android:label="@string/title_activity_select_number_of_groups" >
        </activity>
        <activity
            android:name=".groupReady"
            android:label="@string/title_activity_group_ready" >
        </activity>
        <activity
            android:name=".roundFinished"
            android:label="roundFinished" >
        </activity>
        <activity
            android:name=".moveFinished"
            android:label="moveFinished" >
        </activity>
        <activity
            android:name=".gameFinished"
            android:label="gameFinished" >
        </activity>
        <activity
            android:name=".gameScreen"
            android:label="gameScreen" >
        </activity>
    </application>

</manifest>
